{
  "name": "wakey",
  "private": true,
  "prettier": "@shopify/prettier-config",
  "scripts": {
    "dev": "turbo dev --filter=@wakey/website --filter=@wakey/admin",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "dev:website": "turbo dev --filter=@wakey/website",
    "build:website": "turbo build --filter=@wakey/website",
    "dev:admin": "turbo dev --filter=@wakey/admin",
    "build:admin": "turbo build --filter=@wakey/admin",
    "prepare": "husky"
  },
  "devDependencies": {
    "@shopify/prettier-config": "^1.1.2",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.4.2",
    "turbo": "^2.3.0"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,json,md,mdx,css,yml,yaml}": [
      "prettier --write"
    ]
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=18.0.0"
  }
}
